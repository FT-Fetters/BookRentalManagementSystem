<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyun.bookrentalmanagementsystem.dao.UserInfoDao">
    <select id="findAll" resultType="UserInfo">SELECT * FROM user_info</select>
    <select id="getById" resultType="UserInfo">SELECT * FROM user_info WHERE userId=#{id}</select>
</mapper>